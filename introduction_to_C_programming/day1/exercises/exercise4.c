#include <stdio.h>
int main()
{
  int a=5, b=10;
  double x=25.5, y=20;
  printf("sum of ints %d and %d: %d\n", a,b,a+b);
  printf("product of ints %d and %d: %d\n", a,b,a*b);
  printf("sum of doubles %f and %f: %f\n", x,y,x+y);
  printf("product of doubles %f and %f: %f\n", x,y,x*y);
}
