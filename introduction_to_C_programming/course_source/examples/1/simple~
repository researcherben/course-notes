#include <stdio.h>
int main(){
  int len = 5, wid = 10, tot;
  float average = 0.0;
  tot = len + wid;

  printf("(%d + %d) is %d\n", len, wid, tot); 
  printf("average = %.2f\n", average);
  printf("letter = %c\n", letter);
}
